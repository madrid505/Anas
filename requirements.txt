# المكتبة الأساسية للتعامل مع تلجرام (نسخة مستقرة)
python-telegram-bot==20.6

# لجدولة المهام والنشر التلقائي للأذكار
apscheduler==3.10.4

# للتعامل مع الطلبات الشبكية (ضرورية لعمل المكتبة الأساسية)
httpx==0.24.1

# لقراءة متغيرات البيئة من ملف .env (ضرورية للأمان)
python-dotenv==1.0.0

# للتعامل مع قاعدة البيانات بشكل أسرع (اختيارية ولكن يفضل وجودها)
aiosqlite==0.19.0
